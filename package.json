{
  "name": "haloidergisi_new",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "db:update": "turbo run db:update",
    "dev": "turbo run dev",
    "format": "oxfmt",
    "lint": "oxlint",
    "prepare": "husky",
    "start": "turbo run start",
    "type-check": "oxlint --type-check"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "oxfmt": "^0.26.0",
    "oxlint": "^1.41.0",
    "oxlint-tsgolint": "^0.11.1",
    "turbo": "^2.7.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "oxfmt --write",
      "oxlint --fix",
      "git add"
    ]
  },
  "packageManager": "bun@1.3.5"
}
